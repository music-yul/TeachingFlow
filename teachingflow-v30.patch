diff --git a/src/App.css b/src/App.css
index 04618a5..61dd36c 100644
--- a/src/App.css
+++ b/src/App.css
@@ -195,8 +195,8 @@ button:disabled { opacity: 0.45; cursor: not-allowed; }
 .calendar-day.outside { background: var(--subtle); color: #b6bcc7; }
 .calendar-day.today { background: var(--today); }
 .calendar-day-head { display: flex; align-items: center; justify-content: space-between; }
-.calendar-day-head > strong { font-size: 0.74rem; }
-.calendar-day-actions { display: flex; align-items: center; gap: 0.2rem; }
+.calendar-day-head strong { font-size: 0.74rem; }
+.calendar-day-date { display: flex; align-items: center; gap: 0.15rem; }
 .day-note-add {
   background: none; border: none; color: var(--muted); font-size: 0.85rem; line-height: 1;
   padding: 0 0.15rem; opacity: 0.45;
@@ -514,8 +514,8 @@ thead th small { display: block; font-weight: 400; }
 
 /* ---------- 공휴일 ---------- */
 
-.calendar-day.holiday .calendar-day-head > strong { color: var(--danger); }
-.calendar-day.saturday .calendar-day-head > strong { color: #4a72b8; }
+.calendar-day.holiday .calendar-day-head strong { color: var(--danger); }
+.calendar-day.saturday .calendar-day-head strong { color: #4a72b8; }
 .calendar-event.holiday-tag { background: #fdecec; color: var(--danger); font-weight: 600; }
 .switch-row { display: flex; align-items: center; gap: 0.4rem; margin-top: 0.6rem; font-size: 0.85rem; color: var(--muted); }
 .switch-row input { margin: 0; }
diff --git a/src/components/CalendarView.tsx b/src/components/CalendarView.tsx
index 87a8a51..64e75e3 100644
--- a/src/components/CalendarView.tsx
+++ b/src/components/CalendarView.tsx
@@ -1,5 +1,5 @@
 import { useState } from 'react'
-import type { AppData, Session } from '../types'
+import type { AppData, PersonalBlock, Session } from '../types'
 import { coversDate, dateKey, effectiveLessonIds, sessionLabel, todayKey } from '../schedule'
 import { linkedEvaluation } from '../evaluation'
 import { holidayName } from '../holidays'
@@ -18,6 +18,7 @@ type Props = {
 export default function CalendarView({ data, sessions, month, setMonth, onSelect, update }: Props) {
   const [noteDate, setNoteDate] = useState<string | null>(null)
   const [extraDate, setExtraDate] = useState<string | null>(null)
+  const [editBlock, setEditBlock] = useState<PersonalBlock | null>(null)
 
   const first = new Date(month.getFullYear(), month.getMonth(), 1)
   const start = new Date(first)
@@ -70,17 +71,17 @@ export default function CalendarView({ data, sessions, month, setMonth, onSelect
           return (
             <div className={classNames.join(' ')} key={key}>
               <div className="calendar-day-head">
-                <strong>{date.getDate()}</strong>
-                <div className="calendar-day-actions">
+                <span className="calendar-day-date">
+                  <strong>{date.getDate()}</strong>
                   <button
                     className="day-note-add no-print"
                     title="수업 시간표 변경"
                     onClick={() => setExtraDate(key)}
                   >
-                    🔄
+                    ⇆
                   </button>
-                  <button className="day-note-add no-print" title="이 날짜에 메모 추가" onClick={() => setNoteDate(key)}>📝</button>
-                </div>
+                </span>
+                <button className="day-note-add no-print" title="이 날짜에 메모 추가" onClick={() => setNoteDate(key)}>+</button>
               </div>
               {holiday && <div className="calendar-event holiday-tag">{holiday}</div>}
               {dayEvents.map(event => (
@@ -128,10 +129,7 @@ export default function CalendarView({ data, sessions, month, setMonth, onSelect
                   className="calendar-lesson"
                   key={block.id}
                   style={{ borderLeftColor: '#8b93a2' }}
-                  onClick={() => {
-                    if (!window.confirm(`"${block.title}"(${block.period}교시) 개인 일정을 삭제할까요?`)) return
-                    update({ personalBlocks: data.personalBlocks.filter(item => item.id !== block.id) })
-                  }}
+                  onClick={() => setEditBlock(block)}
                 >
                   <span className="cl-left">
                     {block.period}교시
@@ -163,6 +161,17 @@ export default function CalendarView({ data, sessions, month, setMonth, onSelect
           onCreated={id => { setExtraDate(null); onSelect(id) }}
         />
       )}
+
+      {editBlock && (
+        <AddExtraSessionModal
+          date={editBlock.date}
+          data={data}
+          update={update}
+          editBlock={editBlock}
+          onClose={() => setEditBlock(null)}
+          onCreated={() => setEditBlock(null)}
+        />
+      )}
     </section>
   )
 }
@@ -173,21 +182,33 @@ function AddExtraSessionModal({
   update,
   onClose,
   onCreated,
+  editBlock,
 }: {
   date: string
   data: AppData
   update: (change: Partial<AppData>) => void
   onClose: () => void
   onCreated: (id: string) => void
+  editBlock?: PersonalBlock
 }) {
   const active = data.classes.filter(item => !item.archived)
   const [kind, setKind] = useState<'class' | 'personal'>('class')
   const [classId, setClassId] = useState(active[0]?.id || '')
-  const [period, setPeriod] = useState(1)
-  const [note, setNote] = useState('보강')
-  const [title, setTitle] = useState('')
+  const [period, setPeriod] = useState(editBlock?.period ?? 1)
+  const [note, setNote] = useState(editBlock?.note ?? '보강')
+  const [title, setTitle] = useState(editBlock?.title ?? '')
 
   const create = () => {
+    if (editBlock) {
+      if (!title.trim()) return
+      update({
+        personalBlocks: data.personalBlocks.map(item =>
+          item.id === editBlock.id ? { ...item, period, title: title.trim(), note: note.trim() || undefined } : item,
+        ),
+      })
+      onClose()
+      return
+    }
     if (kind === 'class') {
       if (!classId) return
       const id = makeId('extra')
@@ -201,37 +222,48 @@ function AddExtraSessionModal({
     onClose()
   }
 
+  const remove = () => {
+    if (!editBlock) return
+    if (!window.confirm(`"${editBlock.title}"(${editBlock.period}교시) 개인 일정을 삭제할까요?`)) return
+    update({ personalBlocks: data.personalBlocks.filter(item => item.id !== editBlock.id) })
+    onClose()
+  }
+
   return (
     <div className="modal-backdrop" onClick={onClose}>
       <section className="modal narrow" onClick={event => event.stopPropagation()}>
         <header className="modal-head">
           <div>
-            <p className="eyebrow">수업 시간표 변경</p>
+            <p className="eyebrow">{editBlock ? '개인 일정 수정' : '수업 시간표 변경'}</p>
             <h2>{date}</h2>
           </div>
           <button className="ghost-button" onClick={onClose}>닫기</button>
         </header>
         <div className="modal-body">
-          <p className="hint">
-            정규 시간표와 다른 수업이 있는 경우 해당 날짜의 시간표를 변경합니다.<br />
-            보강 수업을 추가하거나, 다른 교시로 이동한 수업을 등록할 수 있습니다.
-          </p>
-          <p className="hint">
-            💡 수업을 다른 교시로 옮긴 경우<br />
-            이동한 교시에 수업을 추가하고, 원래 교시는 &lsquo;수업 없음&rsquo;으로 변경해 주세요.
-          </p>
-          <div className="mode-picker">
-            <button className={kind === 'class' ? 'mode on' : 'mode'} onClick={() => setKind('class')}>
-              <b>내 학급 수업</b>
-              <small>등록된 학급 중 하나를 골라 진도·채점까지 이어서 기록</small>
-            </button>
-            <button className={kind === 'personal' ? 'mode on' : 'mode'} onClick={() => setKind('personal')}>
-              <b>개인 일정</b>
-              <small>다른 선생님 수업 보강, 회의 등 학급에 속하지 않는 일정</small>
-            </button>
-          </div>
+          {!editBlock && (
+            <>
+              <p className="hint">
+                정규 시간표와 다른 수업이 있는 경우 해당 날짜의 시간표를 변경합니다.<br />
+                보강 수업을 추가하거나, 다른 교시로 이동한 수업을 등록할 수 있습니다.
+              </p>
+              <p className="hint">
+                <b>💡 수업을 다른 교시로 옮기는 경우</b><br />
+                이동한 교시에 수업을 추가하고, 원래 교시는 &lsquo;수업 없음&rsquo;으로 변경해 주세요.
+              </p>
+              <div className="mode-picker">
+                <button className={kind === 'class' ? 'mode on' : 'mode'} onClick={() => setKind('class')}>
+                  <b>내 학급 수업</b>
+                  <small>등록된 학급 중 하나를 골라 진도·채점까지 이어서 기록</small>
+                </button>
+                <button className={kind === 'personal' ? 'mode on' : 'mode'} onClick={() => setKind('personal')}>
+                  <b>개인 일정</b>
+                  <small>다른 선생님 수업 보강, 회의 등 학급에 속하지 않는 일정</small>
+                </button>
+              </div>
+            </>
+          )}
 
-          {kind === 'class' && (
+          {!editBlock && kind === 'class' && (
             <div className="field-grid">
               {!active.length && <p className="hint">등록된 학급이 없습니다.</p>}
               {active.length > 0 && (
@@ -260,11 +292,11 @@ function AddExtraSessionModal({
             </div>
           )}
 
-          {kind === 'personal' && (
+          {(editBlock || kind === 'personal') && (
             <div className="field-grid">
               <label>
                 제목
-                <input value={title} placeholder="예: 2학년 3반 보강(김OO 선생님)" onChange={event => setTitle(event.target.value)} />
+                <input value={title} placeholder="예: 2-1 대강 (김○○T)" onChange={event => setTitle(event.target.value)} />
               </label>
               <label>
                 교시
@@ -272,19 +304,20 @@ function AddExtraSessionModal({
               </label>
               <label>
                 메모
-                <input value={note} placeholder="선택" onChange={event => setNote(event.target.value)} />
+                <input value={note} placeholder="예: 김○○T 병결로 인한 수업 대체" onChange={event => setNote(event.target.value)} />
               </label>
             </div>
           )}
         </div>
         <footer className="modal-foot">
+          {editBlock && <button className="ghost-button danger-button" onClick={remove}>삭제</button>}
           <button className="ghost-button" onClick={onClose}>취소</button>
           <button
             className="primary-button"
             onClick={create}
-            disabled={kind === 'class' ? !classId : !title.trim()}
+            disabled={editBlock ? !title.trim() : (kind === 'class' ? !classId : !title.trim())}
           >
-            {kind === 'class' ? '추가하고 채점표·진도 열기' : '추가'}
+            {editBlock ? '저장' : kind === 'class' ? '추가하고 채점표·진도 열기' : '추가'}
           </button>
         </footer>
       </section>
