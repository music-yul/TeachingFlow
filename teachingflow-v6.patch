diff --git a/src/App.css b/src/App.css
index 8acd82c..9c591ec 100644
--- a/src/App.css
+++ b/src/App.css
@@ -129,10 +129,11 @@ button:disabled { opacity: 0.45; cursor: not-allowed; }
 
 .content { flex: 1; min-width: 0; }
 
-/* 달력 탭에서만 미니 시간표를 함께 보여준다 */
-.calendar-layout { display: flex; align-items: flex-start; gap: 1rem; }
-.calendar-layout .mini-timetable { flex: 0 0 16rem; position: sticky; top: 1rem; margin-top: 0; }
-.calendar-layout .calendar-panel { flex: 1; min-width: 0; }
+/* 달력·진도표 탭에서만 미니 시간표를 함께 보여준다 */
+.timetable-layout { display: flex; align-items: flex-start; gap: 1rem; }
+.timetable-layout .mini-timetable { flex: 0 0 16rem; position: sticky; top: 1rem; margin-top: 0; }
+.timetable-layout > .calendar-panel,
+.timetable-layout > .panel { flex: 1; min-width: 0; }
 .eyebrow { margin: 0; color: var(--muted); font-size: 0.8rem; letter-spacing: 0.02em; }
 
 .panel { background: var(--card); border: 1px solid var(--line); border-radius: 0.6rem; padding: 1rem; max-width: 100%; min-width: 0; }
@@ -141,6 +142,14 @@ button:disabled { opacity: 0.45; cursor: not-allowed; }
 .block:first-child { border-top: none; padding-top: 0; }
 .block h2, .block h3 { margin: 0 0 0.5rem; font-size: 1rem; }
 
+.sns-links { display: flex; gap: 0.5rem; margin-top: 0.6rem; flex-wrap: wrap; }
+.sns-link {
+  display: inline-flex; align-items: center;
+  background: var(--card); border: 1px solid var(--line); border-radius: 999px;
+  padding: 0.3rem 0.9rem; font-size: 0.82rem; color: var(--ink);
+}
+.sns-link:hover { border-color: var(--accent); color: var(--accent); }
+
 .hint { color: var(--muted); font-size: 0.85rem; margin: 0.35rem 0; }
 .warn { color: var(--danger); }
 
@@ -215,7 +224,7 @@ th, td { border: 1px solid var(--line); padding: 0.4rem 0.5rem; text-align: left
 thead th { background: var(--subtle); font-size: 0.82rem; color: var(--muted); }
 thead th small { display: block; font-weight: 400; }
 
-.progress-table .sticky-col { position: sticky; left: 0; background: var(--card); min-width: 13rem; z-index: 1; }
+.progress-table .sticky-col { position: sticky; left: 0; background: var(--card); min-width: 9rem; z-index: 1; }
 .progress-table thead .sticky-col { background: var(--subtle); }
 .progress-table .sticky-col b { display: block; font-weight: 600; }
 .type-chip { display: inline-block; color: var(--card); font-size: 0.68rem; padding: 0.05rem 0.35rem; border-radius: 0.2rem; margin-bottom: 0.15rem; }
@@ -234,8 +243,6 @@ thead th small { display: block; font-weight: 400; }
 .check.big { font-size: 0.95rem; margin: 0.5rem 0; }
 .memo-button { background: none; border: 1px dashed var(--line); color: var(--muted); font-size: 0.75rem; width: 100%; text-align: left; padding: 0.15rem 0.3rem; margin-top: 0.2rem; }
 
-.coverage { margin-top: 1rem; }
-.coverage table { max-width: 34rem; }
 
 
 /* ---------- classes / lessons / events ---------- */
@@ -269,8 +276,8 @@ thead th small { display: block; font-weight: 400; }
 .subject-remove { flex: 0 0 auto; background: none; border: none; color: var(--muted); font-size: 1.05rem; line-height: 1; padding: 0 0.15rem; }
 .subject-remove:hover { color: var(--danger); }
 
-.class-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.6rem; margin-top: 0.5rem; align-items: start; }
-.class-block { border: 1px solid var(--line); border-radius: 0.5rem; padding: 0.6rem; min-width: 0; }
+.class-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.6rem; margin-top: 0.5rem; align-items: start; }
+.class-block { border: 1px solid var(--line); border-radius: 0.5rem; padding: 0.6rem; min-width: 0; container-type: inline-size; }
 
 
 /* ---------- sub tabs ---------- */
@@ -335,7 +342,8 @@ thead th small { display: block; font-weight: 400; }
 
 /* ---------- attendance ---------- */
 
-.session-title { margin: 0.3rem 0 0.6rem; font-size: 1.02rem; }
+.session-title { margin: 0.3rem 0 0.6rem; font-size: 1.02rem; display: flex; align-items: center; gap: 0.6rem; flex-wrap: wrap; }
+.digest-download-open { margin-left: auto; font-size: 0.8rem; }
 .attendance-table .status-cell { white-space: nowrap; }
 .attendance-table .status-cell .slot { width: auto; padding: 0 0.35rem; margin-right: 0.1rem; }
 .attendance-table td input { width: 100%; }
@@ -370,7 +378,7 @@ thead th small { display: block; font-weight: 400; }
 .progress-table { table-layout: fixed; }
 
 @media (min-width: 561px) {
-  .progress-table .sticky-col { width: 15rem; }
+  .progress-table .sticky-col { width: 10.5rem; }
   .progress-table thead th:not(.sticky-col) { width: 9.5rem; }
   .progress-cell { width: 9.5rem; max-width: 9.5rem; }
 }
@@ -448,6 +456,8 @@ thead th small { display: block; font-weight: 400; }
 .calendar-lesson.marked.done span { font-weight: 400; }
 .progress-table tr.marked-row .sticky-col { background: var(--mark); }
 .progress-table tr.marked-row .sticky-col b { font-weight: 700; }
+.progress-table tfoot th, .progress-table tfoot td { border-top: 2px solid var(--line); font-weight: 600; }
+.progress-table tfoot td.warn { color: var(--danger); }
 
 /* ---------- 출석부 날짜 이동 ---------- */
 
@@ -663,6 +673,13 @@ thead th small { display: block; font-weight: 400; }
 
 .slot-grid { display: flex; gap: 0.4rem; margin-top: 0.5rem; flex-wrap: wrap; }
 .day-column { display: flex; flex-direction: column; gap: 0.15rem; align-items: center; }
+
+/* 한 열에 시간표 4개를 놓으면 칸이 좁아지므로, 좁을 땐 요일을 세로로 쌓고 교시를 가로로 배치한다 */
+@container (max-width: 230px) {
+  .slot-grid { flex-direction: column; gap: 0.2rem; }
+  .day-column { flex-direction: row; align-items: center; gap: 0.3rem; }
+  .day-column b { flex: 0 0 1rem; text-align: center; }
+}
 .day-column b { font-size: 0.78rem; color: var(--muted); }
 .slot { width: 1.9rem; height: 1.7rem; border: 1px solid var(--line); background: var(--card); font-size: 0.78rem; padding: 0; }
 .slot.on { background: var(--accent); border-color: var(--accent); color: var(--on-accent); }
@@ -676,8 +693,9 @@ thead th small { display: block; font-weight: 400; }
 .order { flex: 0 0 1.6rem; text-align: center; color: var(--muted); font-size: 0.85rem; padding-top: 0.4rem; }
 .lesson-fields { flex: 1; display: flex; flex-direction: column; gap: 0.25rem; min-width: 0; }
 .lesson-line { display: flex; gap: 0.35rem; min-width: 0; flex-wrap: wrap; }
-.lesson-line input { flex: 1 1 9rem; min-width: 0; }
-.lesson-line select { flex: 0 1 auto; min-width: 0; }
+.lesson-line .lesson-type-select { flex: 0 0 6.5rem; min-width: 0; }
+.lesson-line .lesson-title-input { flex: 1 1 11rem; min-width: 0; }
+.lesson-line .note-input { flex: 1 1 14rem; min-width: 0; }
 .note-input { font-size: 0.82rem; }
 .row-actions { display: flex; gap: 0.2rem; }
 
@@ -830,8 +848,8 @@ thead th small { display: block; font-weight: 400; }
   .tab-item { padding: 0.35rem 0.7rem; font-size: 0.88rem; }
   .tab-index { display: none; }
 
-  .calendar-layout { flex-direction: column; }
-  .calendar-layout .mini-timetable { flex: none; position: static; width: 100%; }
+  .timetable-layout { flex-direction: column; }
+  .timetable-layout .mini-timetable { flex: none; position: static; width: 100%; }
 
   .topbar { padding: 0.5rem 0.7rem; gap: 0.4rem; }
   .brand-mark { width: 1.9rem; height: 1.9rem; font-size: 1.1rem; }
@@ -892,7 +910,7 @@ thead th small { display: block; font-weight: 400; }
 
 @media (max-width: 560px) {
   .slot { width: 1.7rem; height: 1.5rem; font-size: 0.72rem; }
-  .progress-table .sticky-col { width: 10rem; }
+  .progress-table .sticky-col { width: 8rem; }
   .progress-table thead th:not(.sticky-col) { width: 8rem; }
   .progress-cell { width: 8rem; max-width: 8rem; }
   .subtabs { overflow-x: auto; flex-wrap: nowrap; padding-bottom: 0.2rem; }
diff --git a/src/App.tsx b/src/App.tsx
index 2db4021..be6f90c 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -14,7 +14,7 @@ import ImportModal from './components/ImportModal'
 import MiniTimetable from './components/MiniTimetable'
 import './App.css'
 
-const tabs = ['달력', '진도표', '반별 출석부', '평가 관리', '설정'] as const
+const tabs = ['달력', '진도표', '반별 출석부', '평가', '설정'] as const
 type Tab = (typeof tabs)[number]
 
 export default function App() {
@@ -24,7 +24,7 @@ export default function App() {
   const openEvaluation = (evaluationId: string, classId: string) => {
     setSelected(null)
     setEvalJump({ evaluationId, classId })
-    setTab('평가 관리')
+    setTab('평가')
   }
   const [month, setMonth] = useState(() => new Date())
   const [selected, setSelected] = useState<string | null>(null)
@@ -177,14 +177,19 @@ export default function App() {
           )}
 
           {tab === '달력' && (
-            <div className="calendar-layout">
+            <div className="timetable-layout">
               <MiniTimetable data={data} />
               <CalendarView data={data} sessions={sessions} month={month} setMonth={setMonth} onSelect={setSelected} update={update} />
             </div>
           )}
-          {tab === '진도표' && <ProgressView data={data} sessions={sessions} update={update} onSelect={setSelected} onOpenEvaluation={openEvaluation} />}
+          {tab === '진도표' && (
+            <div className="timetable-layout">
+              <MiniTimetable data={data} />
+              <ProgressView data={data} sessions={sessions} update={update} onSelect={setSelected} onOpenEvaluation={openEvaluation} />
+            </div>
+          )}
           {tab === '반별 출석부' && <AttendanceView data={data} sessions={sessions} update={update} />}
-          {tab === '평가 관리' && (
+          {tab === '평가' && (
             <EvaluationsView
               data={data}
               update={update}
diff --git a/src/components/AttendanceView.tsx b/src/components/AttendanceView.tsx
index 4467233..93716de 100644
--- a/src/components/AttendanceView.tsx
+++ b/src/components/AttendanceView.tsx
@@ -1,7 +1,7 @@
 import { useState } from 'react'
 import type { AppData, AttendanceStatus, Session } from '../types'
 import { dateKey, formatShort, sessionLabel, todayKey } from '../schedule'
-import { exportClassDigestXlsx } from '../exportPrint'
+import { exportClassDigestXlsx, exportStudentDigestXlsx } from '../exportPrint'
 import { studentLabel } from '../students'
 
 type Props = {
@@ -28,6 +28,8 @@ export default function AttendanceView({ data, sessions, update }: Props) {
   const [date, setDate] = useState(todayKey())
   const [classFilter, setClassFilter] = useState('')
   const [activeId, setActiveId] = useState<string | null>(null)
+  const [digestStudentId, setDigestStudentId] = useState<string | null>(null)
+  const [showDownload, setShowDownload] = useState(false)
 
   if (!active.length) {
     return (
@@ -108,6 +110,7 @@ export default function AttendanceView({ data, sessions, update }: Props) {
               {current.period}교시 {classroom.name}
               {current.swappedFrom && <em className="swap-tag">{current.swappedFrom}요일 대체</em>}
               {' — '}{sessionLabel(data, current)}
+              <button className="ghost-button digest-download-open" onClick={() => setShowDownload(true)}>누가기록 다운로드</button>
             </h3>
 
             {!classroom.students.length && <p className="hint">학생 명단이 비어 있습니다.</p>}
@@ -122,8 +125,9 @@ export default function AttendanceView({ data, sessions, update }: Props) {
                       <th>학번</th>
                       <th>성명</th>
                       <th>출결</th>
-                      <th>수업 중 특이사항</th>
+                      <th>수업 중 특기사항</th>
                       <th>누계</th>
+                      <th>누가기록</th>
                     </tr>
                   </thead>
                   <tbody>
@@ -155,7 +159,7 @@ export default function AttendanceView({ data, sessions, update }: Props) {
                           <td>
                             <input
                               value={data.activities[key] || ''}
-                              placeholder="관찰 내용, 활동 특이사항"
+                              placeholder="관찰 내용, 활동 특기사항"
                               onChange={event => update({ activities: { ...data.activities, [key]: event.target.value } })}
                             />
                           </td>
@@ -163,6 +167,9 @@ export default function AttendanceView({ data, sessions, update }: Props) {
                             {absent > 0 && <span className="warn">결석 {absent}</span>}
                             {late > 0 && <span>지각 {late}</span>}
                           </td>
+                          <td>
+                            <button className="ghost-button" onClick={() => setDigestStudentId(student.id)}>보기</button>
+                          </td>
                         </tr>
                       )
                     })}
@@ -175,68 +182,133 @@ export default function AttendanceView({ data, sessions, update }: Props) {
         )
       })()}
 
-      <StudentDigest data={data} sessions={sessions} classes={active} />
+      {digestStudentId && current && (() => {
+        const classroom = data.classes.find(item => item.id === current.classId)
+        const student = classroom?.students.find(item => item.id === digestStudentId)
+        if (!classroom || !student) return null
+        return (
+          <StudentDigestModal
+            data={data}
+            sessions={sessions}
+            classroom={classroom}
+            student={student}
+            onClose={() => setDigestStudentId(null)}
+          />
+        )
+      })()}
+
+      {showDownload && current && (() => {
+        const classroom = data.classes.find(item => item.id === current.classId)
+        if (!classroom) return null
+        return (
+          <DigestDownloadModal
+            classroom={classroom}
+            onClose={() => setShowDownload(false)}
+            onDownload={studentId => {
+              if (studentId) exportStudentDigestXlsx(data, sessions, classroom.id, studentId)
+              else exportClassDigestXlsx(data, sessions, classroom.id)
+              setShowDownload(false)
+            }}
+          />
+        )
+      })()}
     </section>
   )
 }
 
-function StudentDigest({ data, sessions, classes }: { data: AppData; sessions: Session[]; classes: AppData['classes'] }) {
-  const [classId, setClassId] = useState(classes[0]?.id || '')
-  const [studentId, setStudentId] = useState('')
-  const classroom = classes.find(item => item.id === classId)
-  const student = classroom?.students.find(item => item.id === studentId)
-
-  const own = classroom
-    ? sessions.filter(item => item.classId === classroom.id && item.mode !== 'none' && !item.cancelled)
-    : []
-
-  const notes = student
-    ? own
-        .map(item => ({
-          session: item,
-          note: data.activities[`${item.id}:${student.id}`] || '',
-          status: data.attendance[`${item.id}:${student.id}`],
-        }))
-        .filter(item => item.note || (item.status && item.status !== '출석'))
-    : []
+/** 학생 한 명의 학기 전체 출결·특기사항을 모아 보여주는 창. 세특 쓸 때 근거로 쓴다. */
+function StudentDigestModal({
+  data,
+  sessions,
+  classroom,
+  student,
+  onClose,
+}: {
+  data: AppData
+  sessions: Session[]
+  classroom: AppData['classes'][number]
+  student: AppData['classes'][number]['students'][number]
+  onClose: () => void
+}) {
+  const own = sessions
+    .filter(item => item.classId === classroom.id && item.mode !== 'none' && !item.cancelled)
+    .sort((left, right) => (left.date + String(left.period)).localeCompare(right.date + String(right.period)))
+
+  const notes = own
+    .map(item => ({
+      session: item,
+      note: data.activities[`${item.id}:${student.id}`] || '',
+      status: data.attendance[`${item.id}:${student.id}`],
+    }))
+    .filter(item => item.note || (item.status && item.status !== '출석'))
 
   return (
-    <div className="block">
-      <h3>학생별 기록 모아 보기</h3>
-      <p className="hint">
-        한 학생의 학기 전체 기록을 모아 봅니다. 세특 쓰실 때 근거로 쓰세요.
-        아래 학급을 골라두면 그 반 <b>전체 학생</b>의 기록을 한 번에 엑셀로도 받을 수 있습니다.
-      </p>
-      <div className="inline-form">
-        <select value={classId} onChange={event => { setClassId(event.target.value); setStudentId('') }}>
-          {classes.map(item => {
-            const subject = data.subjects.find(value => value.id === item.subjectId)
-            return <option key={item.id} value={item.id}>{subject?.name} {item.name}</option>
-          })}
-        </select>
-        <select value={studentId} onChange={event => setStudentId(event.target.value)}>
-          <option value="">학생 선택</option>
-          {classroom?.students.map(item => (
-            <option key={item.id} value={item.id}>{studentLabel(item)}</option>
+    <div className="modal-backdrop" onClick={onClose}>
+      <section className="modal" onClick={event => event.stopPropagation()}>
+        <header className="modal-head">
+          <div>
+            <p className="eyebrow">누가기록</p>
+            <h2>{classroom.name} · {studentLabel(student)}</h2>
+          </div>
+          <button className="ghost-button" onClick={onClose}>닫기</button>
+        </header>
+        <div className="modal-body">
+          {!notes.length && <p className="hint">쌓인 기록이 없습니다.</p>}
+          {notes.map(item => (
+            <div className="digest-row" key={item.session.id}>
+              <b>{formatShort(item.session.date)}</b>
+              <span className="digest-lesson">{sessionLabel(data, item.session)}</span>
+              {item.status && item.status !== '출석' && <span className="warn">{item.status}</span>}
+              <span>{item.note}</span>
+            </div>
           ))}
-        </select>
-        <button
-          className="ghost-button"
-          disabled={!classroom}
-          onClick={() => classroom && exportClassDigestXlsx(data, sessions, classroom.id)}
-        >
-          이 반 전체 기록 엑셀로 내려받기
-        </button>
-      </div>
-      {student && !notes.length && <p className="hint">쌓인 기록이 없습니다.</p>}
-      {notes.map(item => (
-        <div className="digest-row" key={item.session.id}>
-          <b>{formatShort(item.session.date)}</b>
-          <span className="digest-lesson">{sessionLabel(data, item.session)}</span>
-          {item.status && item.status !== '출석' && <span className="warn">{item.status}</span>}
-          <span>{item.note}</span>
         </div>
-      ))}
+      </section>
+    </div>
+  )
+}
+
+/** 누가기록을 엑셀로 내려받을 대상(반 전체 / 학생 한 명)을 고르는 창. */
+function DigestDownloadModal({
+  classroom,
+  onClose,
+  onDownload,
+}: {
+  classroom: AppData['classes'][number]
+  onClose: () => void
+  onDownload: (studentId: string | null) => void
+}) {
+  const [studentId, setStudentId] = useState('')
+
+  return (
+    <div className="modal-backdrop" onClick={onClose}>
+      <section className="modal narrow" onClick={event => event.stopPropagation()}>
+        <header className="modal-head">
+          <div>
+            <p className="eyebrow">누가기록 다운로드</p>
+            <h2>{classroom.name}</h2>
+          </div>
+          <button className="ghost-button" onClick={onClose}>닫기</button>
+        </header>
+        <div className="modal-body">
+          <p className="hint">반 전체를 한 시트로 받거나, 학생 한 명만 골라 받을 수 있습니다.</p>
+          <div className="field-grid">
+            <label>
+              대상 학생 (선택 안 하면 반 전체)
+              <select value={studentId} onChange={event => setStudentId(event.target.value)}>
+                <option value="">전체 학생</option>
+                {classroom.students.map(item => (
+                  <option key={item.id} value={item.id}>{studentLabel(item)}</option>
+                ))}
+              </select>
+            </label>
+          </div>
+        </div>
+        <footer className="modal-foot">
+          <button className="ghost-button" onClick={onClose}>취소</button>
+          <button className="primary-button" onClick={() => onDownload(studentId || null)}>엑셀로 내려받기</button>
+        </footer>
+      </section>
     </div>
   )
 }
diff --git a/src/components/EventsView.tsx b/src/components/EventsView.tsx
index bf5b6d1..fb7776c 100644
--- a/src/components/EventsView.tsx
+++ b/src/components/EventsView.tsx
@@ -49,6 +49,7 @@ const emptyDraft: Draft = {
 
 export default function EventsView({ data, update }: Props) {
   const [draft, setDraft] = useState<Draft>(emptyDraft)
+  const edit = (change: Partial<AppData['settings']>) => update({ settings: { ...data.settings, ...change } })
 
   const addEvent = () => {
     if (!draft.date || !draft.title.trim()) return
@@ -72,6 +73,30 @@ export default function EventsView({ data, update }: Props) {
 
   return (
     <section className="panel">
+      <div className="block">
+        <h2>학기 설정</h2>
+        <p className="hint">
+          여기 입력한 기간 안에서만 수업이 배정됩니다. 학기가 바뀌면 날짜를 바꿔 주세요.
+        </p>
+        <div className="field-grid">
+          <label>학교명<input value={data.settings.schoolName} onChange={event => edit({ schoolName: event.target.value })} /></label>
+          <label>교사명<input value={data.settings.teacherName} onChange={event => edit({ teacherName: event.target.value })} /></label>
+          <label>학년도<input value={data.settings.year} onChange={event => edit({ year: event.target.value })} /></label>
+          <label>학기 이름<input value={data.settings.termName} onChange={event => edit({ termName: event.target.value })} /></label>
+          <label>학기 시작<input type="date" value={data.settings.termStart} onChange={event => edit({ termStart: event.target.value })} /></label>
+          <label>학기 종료<input type="date" value={data.settings.termEnd} onChange={event => edit({ termEnd: event.target.value })} /></label>
+        </div>
+        <label className="switch-row">
+          <input
+            type="checkbox"
+            checked={data.settings.useHolidays !== false}
+            onChange={event => edit({ useHolidays: event.target.checked })}
+          />
+          대한민국 공휴일을 달력에 표시하고 그날 수업을 자동으로 뺍니다
+        </label>
+      </div>
+
+      <div className="block">
       <h2>학사일정</h2>
       <p className="hint">
         휴업일과 행사를 등록하면 그 시간은 진도 배정에서 빠지고, 뒤 차시가 자동으로 밀립니다.<br />
@@ -219,6 +244,7 @@ export default function EventsView({ data, update }: Props) {
         )
       })}
       {!sorted.length && <p className="hint">등록된 일정이 없습니다.</p>}
+      </div>
     </section>
   )
 }
diff --git a/src/components/HelpView.tsx b/src/components/HelpView.tsx
index 5a705ea..e13829b 100644
--- a/src/components/HelpView.tsx
+++ b/src/components/HelpView.tsx
@@ -1,7 +1,7 @@
 import { useState } from 'react'
 import { APP_NAME, APP_TAGLINE, APP_VERSION } from '../storage'
 
-const sections = ['처음 설정', '매일 쓰는 법', '평가 관리', '계획이 틀어졌을 때', '백업', '문제 해결'] as const
+const sections = ['처음 설정', '매일 쓰는 법', '평가', '계획이 틀어졌을 때', '데이터 관리', '문제 해결'] as const
 type Section = (typeof sections)[number]
 
 export default function HelpView() {
@@ -23,7 +23,7 @@ export default function HelpView() {
 
           <h3>1단계 · 학기 기간 정하기</h3>
           <p>
-            <b>설정 › 학기·백업</b>에서 학교명, 교사명, 학년도, 학기 이름, 학기 시작일과 종료일을 넣습니다.
+            <b>설정 › 학사일정</b>에서 학교명, 교사명, 학년도, 학기 이름, 학기 시작일과 종료일을 넣습니다.
             학교명은 왼쪽 주간 시간표 제목에도 그대로 쓰이니, 줄여 쓰고 싶으면 여기에 직접 줄여서 입력하시면 됩니다.
           </p>
           <p className="callout">
@@ -59,18 +59,18 @@ export default function HelpView() {
           <table>
             <thead><tr><th>상태</th><th>동작</th></tr></thead>
             <tbody>
-              <tr><td>켬</td><td>진도표와 수업 목록에 나옵니다.</td></tr>
-              <tr><td>끔</td><td>진도표·수업 목록에서 빠지고, 달력과 반별 출석부에는 그대로 나옵니다.</td></tr>
+              <tr><td>켬</td><td>진도표와 진도표 관리에 나옵니다.</td></tr>
+              <tr><td>끔</td><td>진도표·진도표 관리에서 빠지고, 달력과 반별 출석부에는 그대로 나옵니다.</td></tr>
             </tbody>
           </table>
           <p>
-            CA, HR처럼 진도를 따로 관리하지 않는 시간은 꺼두세요. 출결과 특이사항 기록은 그대로 됩니다.
+            CA, HR처럼 진도를 따로 관리하지 않는 시간은 꺼두세요. 출결과 특기사항 기록은 그대로 됩니다.
             체크할 진도가 없는 대신, 달력에서 <b>지난 날짜의 수업은 자동으로 완료(회색)처럼 표시</b>됩니다.
           </p>
 
-          <h3>4단계 · 수업 목록 등록하기</h3>
+          <h3>4단계 · 진도표 관리 등록하기</h3>
           <p>
-            <b>설정 › 수업 목록</b>을 열면 <b>수업 유형</b>(가창·연주·감상 등)이 맨 위에 있습니다. 먼저 필요한 유형을 만들거나 확인하세요.
+            <b>설정 › 진도표 관리</b>을 열면 <b>수업 유형</b>(가창·연주·감상 등)이 맨 위에 있습니다. 먼저 필요한 유형을 만들거나 확인하세요.
             유형 중 <b>강조</b>를 켠 것(기본은 수행평가)은 달력·진도표에서 굵은 글씨와 다른 배경으로 눈에 띄게 표시됩니다.
           </p>
           <p>
@@ -82,7 +82,7 @@ export default function HelpView() {
           <h3>5단계 · 학사일정 넣기</h3>
           <p className="callout">
             신정·설날·추석·공휴일과 대체공휴일은 <b>이미 들어 있습니다.</b> 따로 등록하지 않으셔도 달력에 빨갛게 표시되고 그날 수업이 자동으로 빠집니다.
-            음력 명절은 천문 계산으로 구하므로 연도 제한 없이 몇십 년이 지나도 그대로 맞습니다. 원하지 않으면 설정 › 학기·백업에서 끌 수 있습니다.
+            음력 명절은 천문 계산으로 구하므로 연도 제한 없이 몇십 년이 지나도 그대로 맞습니다. 원하지 않으면 설정 › 학사일정에서 끌 수 있습니다.
             재량휴업일·시험·행사처럼 학교마다 다른 일정만 아래에서 등록하시면 됩니다.
           </p>
           <table>
@@ -122,11 +122,11 @@ export default function HelpView() {
             <li>탭을 열면 <b>오늘 수업이 있는 반</b>이 자동으로 잡힙니다. 그날 수업이 여러 개면 아래에 <b>교시 버튼</b>이 나오고, 가장 이른 교시가 기본으로 열립니다. 버튼을 눌러 다른 수업으로 바꿉니다.</li>
             <li>다른 날을 보려면 <b>하루 전 / 다음 날</b> 화살표를 쓰거나 날짜를 직접 고르세요. 특정 반만 보고 싶으면 오른쪽 <b>학급</b> 선택으로 좁힐 수 있습니다.</li>
             <li>학생별로 출결 버튼을 누릅니다. <b>아무것도 안 누르면 출석으로 봅니다.</b> 같은 버튼을 다시 누르면 취소됩니다.</li>
-            <li>오른쪽 칸에 수업 중 특이사항을 적습니다.</li>
+            <li>오른쪽 칸에 수업 중 특기사항을 적습니다.</li>
             <li>맨 오른쪽에 그 학생의 결석·지각 누계가 표시됩니다.</li>
           </ol>
           <p className="callout">
-            화면 맨 아래 <b>학생별 기록 모아 보기</b>에서 학생 한 명을 고르면, 학기 전체의 특이사항이 날짜·수업명과 함께 정리됩니다.
+            표의 <b>누가기록</b> 칸에서 학생 한 명을 고르면, 학기 전체의 특기사항이 날짜·수업명과 함께 정리됩니다. 표 위쪽 <b>누가기록 다운로드</b>로 반 전체 또는 학생 한 명만 골라 엑셀로 받을 수 있습니다.
             <b>이 반 전체 기록 엑셀로 내려받기</b> 버튼을 누르면 그 반 학생 전원의 기록을 한 파일로 받을 수 있습니다. 세특 쓰실 때 근거 자료로 쓰세요.
           </p>
 
@@ -137,7 +137,7 @@ export default function HelpView() {
             <tbody>
               <tr><td>노란 배경</td><td>오늘 하는 수업</td></tr>
               <tr><td>회색 배경</td><td>완료 표시한 수업</td></tr>
-              <tr><td>굵은 글씨·다른 배경</td><td>수행평가처럼 <b>강조</b>로 지정한 유형입니다. 설정 › 수업 목록에서 바꿉니다.</td></tr>
+              <tr><td>굵은 글씨·다른 배경</td><td>수행평가처럼 <b>강조</b>로 지정한 유형입니다. 설정 › 진도표 관리에서 바꿉니다.</td></tr>
               <tr><td>+ 메모</td><td>누르면 팝업이 열립니다. 길어도 칸은 한 줄로 고정됩니다.</td></tr>
               <tr><td>시간 부족</td><td>학기 안에 이 차시를 할 시간이 없다는 뜻</td></tr>
               <tr><td>엑셀로 내보내기 / 인쇄</td><td>위쪽 버튼으로 지금 과목의 진도표를 엑셀 파일이나 인쇄물로 뽑을 수 있습니다.</td></tr>
@@ -178,13 +178,13 @@ export default function HelpView() {
         </div>
       )}
 
-      {section === '평가 관리' && (
+      {section === '평가' && (
         <div className="help-body">
           <p className="lead">학급·학생 데이터를 그대로 가져다 쓰는 성적 입력 화면입니다. 학생을 따로 등록하지 않습니다.</p>
 
           <h3>평가 만들기</h3>
           <ol>
-            <li><b>평가 관리</b> 탭에서 과목을 고르고 <b>+ 새 평가 만들기</b>를 누릅니다.</li>
+            <li><b>평가</b> 탭에서 과목을 고르고 <b>+ 새 평가 만들기</b>를 누릅니다.</li>
             <li>평가명, 유형, 반영 비율(%), 평가일, 대상 학급을 정합니다. 대상 학급을 비워두면 그 과목의 모든 학급이 대상입니다.</li>
             <li>만들면 <b>평가 설정</b> 탭으로 넘어갑니다. 요소명과 배점을 하나씩 추가하세요. 요소는 평가마다 완전히 다르게 만들 수 있습니다.</li>
           </ol>
@@ -212,9 +212,9 @@ export default function HelpView() {
           <h3>내보내기</h3>
           <p>평가 화면 위쪽 <b>엑셀로 내보내기</b>를 누르면 학급별 시트로 나뉜 파일을 받습니다. 학번·이름·요소별 점수·원점수·반영점수·비고가 들어갑니다.</p>
 
-          <h3>수업 목록에서 평가 연결하기</h3>
+          <h3>진도표 관리에서 평가 연결하기</h3>
           <p>
-            <b>설정 › 수업 목록</b>에서 수업 유형에 <b>강조</b>가 켜져 있으면(기본은 수행평가), 그 유형을 쓰는 차시마다
+            <b>설정 › 진도표 관리</b>에서 수업 유형에 <b>강조</b>가 켜져 있으면(기본은 수행평가), 그 유형을 쓰는 차시마다
             <b>평가 선택</b> 칸이 나타납니다. 여기서 평가를 고르면 그 차시를 쓰는 모든 수업이 그 평가와 연결됩니다.
           </p>
 
@@ -224,7 +224,7 @@ export default function HelpView() {
             <b>수행평가 채점하기</b> 버튼이 뜨고, 누르면 그 학급이 선택된 채로 채점 화면이 바로 열립니다.
           </p>
           <p>
-            수업 목록에서 따로 연결하지 않아도, 평가 관리에서 만든 평가의 날짜·대상 학급이 그날 수업과 우연히 일치하면
+            진도표 관리에서 따로 연결하지 않아도, 평가에서 만든 평가의 날짜·대상 학급이 그날 수업과 우연히 일치하면
             일반 <b>평가</b> 칸으로 안내됩니다. 반대로 평가 화면에도 <b>관련 수업 일정</b>이 보이며, 눌러보면 그 수업의 출석·진도 화면으로 이동합니다.
           </p>
 
@@ -256,7 +256,7 @@ export default function HelpView() {
             눌러서 그 학생의 모든 요소에 한 번에 채울 수 있습니다.
           </p>
           <p className="callout">
-            아직 지원하지 않는 것: 파일 업로드로 채점기준 자동 불러오기, 학생별 화면에서 평가 기록 모아 보기.
+            아직 지원하지 않는 것: 파일 업로드로 채점기준 자동 불러오기.
             지금은 채점기준을 직접 입력해서 등록합니다.
           </p>
         </div>
@@ -293,7 +293,7 @@ export default function HelpView() {
         </div>
       )}
 
-      {section === '백업' && (
+      {section === '데이터 관리' && (
         <div className="help-body">
           <p className="callout warn">
             모든 기록은 이 브라우저 안에만 저장됩니다. 서버에 올라가지 않습니다.
@@ -334,8 +334,8 @@ export default function HelpView() {
           <table>
             <thead><tr><th>증상</th><th>원인과 해결</th></tr></thead>
             <tbody>
-              <tr><td>달력이 텅 비어 있다</td><td>설정 › 학기·백업에서 학기 기간이 맞지 않습니다.</td></tr>
-              <tr><td>진도표에 아무것도 없다</td><td>설정 › 수업 목록에 차시가 없습니다.</td></tr>
+              <tr><td>달력이 텅 비어 있다</td><td>설정 › 학사일정에서 학기 기간이 맞지 않습니다.</td></tr>
+              <tr><td>진도표에 아무것도 없다</td><td>설정 › 진도표 관리에 차시가 없습니다.</td></tr>
               <tr><td>진도표 과목 목록에 특정 과목이 없다</td><td>그 과목의 진도표 사용이 꺼져 있습니다.</td></tr>
               <tr><td>시간표에 붉은 테두리가 생겼다</td><td>같은 시간에 두 반이 잡혀 있습니다.</td></tr>
               <tr><td>미배정이라고 뜬다</td><td>수업 가능한 시간이 등록한 차시보다 많습니다. 차시를 더 넣으세요.</td></tr>
diff --git a/src/components/LessonsView.tsx b/src/components/LessonsView.tsx
index 9ce5da2..6a7376e 100644
--- a/src/components/LessonsView.tsx
+++ b/src/components/LessonsView.tsx
@@ -132,17 +132,25 @@ export default function LessonsView({ data, update }: Props) {
             <span className="order">{index + 1}</span>
             <div className="lesson-fields">
               <div className="lesson-line">
-                <input value={lesson.title} onChange={event => editLesson(lesson.id, { title: event.target.value })} />
-                <select value={lesson.typeId} onChange={event => editLesson(lesson.id, { typeId: event.target.value })}>
+                <select
+                  className="lesson-type-select"
+                  value={lesson.typeId}
+                  onChange={event => editLesson(lesson.id, { typeId: event.target.value })}
+                >
                   {data.types.map(item => <option key={item.id} value={item.id}>{item.name}</option>)}
                 </select>
+                <input
+                  className="lesson-title-input"
+                  value={lesson.title}
+                  onChange={event => editLesson(lesson.id, { title: event.target.value })}
+                />
+                <input
+                  className="note-input"
+                  value={lesson.note}
+                  placeholder="이 차시 공통 메모 (모든 학급에 함께 표시)"
+                  onChange={event => editLesson(lesson.id, { note: event.target.value })}
+                />
               </div>
-              <input
-                className="note-input"
-                value={lesson.note}
-                placeholder="이 차시 공통 메모 (모든 학급에 함께 표시)"
-                onChange={event => editLesson(lesson.id, { note: event.target.value })}
-              />
               {type?.emphasis && (
                 <label className="lesson-eval-pick">
                   평가 선택
diff --git a/src/components/ProgressView.tsx b/src/components/ProgressView.tsx
index 8405248..396a4c5 100644
--- a/src/components/ProgressView.tsx
+++ b/src/components/ProgressView.tsx
@@ -143,36 +143,30 @@ export default function ProgressView({ data, sessions, update, onSelect, onOpenE
                 )
               })}
             </tbody>
-          </table>
-        </div>
-      )}
-
-      {classes.length > 0 && (
-        <div className="coverage">
-          <h3>수업 시간 대비 진도 분량</h3>
-          <table>
-            <thead>
-              <tr><th>학급</th><th>학기 중 수업 시간</th><th>등록된 차시</th><th>여유</th></tr>
-            </thead>
-            <tbody>
-              {classes.map(classroom => {
-                const stat = stats.find(item => item.classId === classroom.id)
-                if (!stat) return null
-                return (
-                  <tr key={classroom.id}>
-                    <td>{classroom.name}</td>
-                    <td>{stat.total}</td>
-                    <td>{stat.lessonCount}</td>
-                    <td className={stat.spare < 0 ? 'warn' : ''}>
-                      {stat.spare >= 0 ? `${stat.spare}시간 남음` : `${-stat.spare}시간 부족`}
+            <tfoot>
+              <tr className="progress-total-row">
+                <th className="sticky-col">총 차시</th>
+                {shownClasses.map(item => {
+                  const stat = stats.find(value => value.classId === item.id)
+                  return <td key={item.id}>{stat ? `${stat.lessonCount}차시` : '-'}</td>
+                })}
+              </tr>
+              <tr className="progress-total-row">
+                <th className="sticky-col">여유</th>
+                {shownClasses.map(item => {
+                  const stat = stats.find(value => value.classId === item.id)
+                  return (
+                    <td className={stat && stat.spare < 0 ? 'warn' : ''} key={item.id}>
+                      {stat ? `${stat.spare}시간` : '-'}
                     </td>
-                  </tr>
-                )
-              })}
-            </tbody>
+                  )
+                })}
+              </tr>
+            </tfoot>
           </table>
         </div>
       )}
+
       {memoTarget && (
         <MemoModal
           title={memoTarget.title}
diff --git a/src/components/SettingsHub.tsx b/src/components/SettingsHub.tsx
index 4e31595..f072bf9 100644
--- a/src/components/SettingsHub.tsx
+++ b/src/components/SettingsHub.tsx
@@ -1,5 +1,5 @@
 import { useState } from 'react'
-import type { AppData, Settings } from '../types'
+import type { AppData } from '../types'
 import { APP_NAME, APP_TAGLINE, APP_VERSION, exportData } from '../storage'
 import ClassesView from './ClassesView'
 import LessonsView from './LessonsView'
@@ -7,7 +7,7 @@ import EventsView from './EventsView'
 import HelpView from './HelpView'
 import AppearanceView from './AppearanceView'
 
-const subTabs = ['학급·시간표', '수업 목록', '학사일정', '화면', '학기·백업', '도움말'] as const
+const subTabs = ['학사일정', '학급·시간표', '진도표 관리', '화면', '데이터 관리', '도움말'] as const
 type SubTab = (typeof subTabs)[number]
 
 type Props = {
@@ -18,9 +18,14 @@ type Props = {
   onReset: () => void
 }
 
+const links = [
+  { label: 'Blog', url: 'https://blog.naver.com/music_yul' },
+  { label: 'Instagram', url: 'https://instagram.com/music_yul' },
+  { label: 'Youtube', url: 'https://youtube.com/@music_yul' },
+]
+
 export default function SettingsHub({ data, update, onFiles, onImport, onReset }: Props) {
-  const [sub, setSub] = useState<SubTab>('학급·시간표')
-  const edit = (change: Partial<Settings>) => update({ settings: { ...data.settings, ...change } })
+  const [sub, setSub] = useState<SubTab>('학사일정')
 
   return (
     <div>
@@ -32,37 +37,14 @@ export default function SettingsHub({ data, update, onFiles, onImport, onReset }
         ))}
       </div>
 
-      {sub === '학급·시간표' && <ClassesView data={data} update={update} onFiles={onFiles} />}
-      {sub === '수업 목록' && <LessonsView data={data} update={update} />}
       {sub === '학사일정' && <EventsView data={data} update={update} />}
+      {sub === '학급·시간표' && <ClassesView data={data} update={update} onFiles={onFiles} />}
+      {sub === '진도표 관리' && <LessonsView data={data} update={update} />}
       {sub === '화면' && <AppearanceView data={data} update={update} />}
       {sub === '도움말' && <HelpView />}
 
-      {sub === '학기·백업' && (
+      {sub === '데이터 관리' && (
         <section className="panel">
-          <div className="block">
-            <h2>학기 설정</h2>
-            <p className="hint">
-              여기 입력한 기간 안에서만 수업이 배정됩니다. 학기가 바뀌면 날짜를 바꿔 주세요.
-            </p>
-            <div className="field-grid">
-              <label>학교명<input value={data.settings.schoolName} onChange={event => edit({ schoolName: event.target.value })} /></label>
-              <label>교사명<input value={data.settings.teacherName} onChange={event => edit({ teacherName: event.target.value })} /></label>
-              <label>학년도<input value={data.settings.year} onChange={event => edit({ year: event.target.value })} /></label>
-              <label>학기 이름<input value={data.settings.termName} onChange={event => edit({ termName: event.target.value })} /></label>
-              <label>학기 시작<input type="date" value={data.settings.termStart} onChange={event => edit({ termStart: event.target.value })} /></label>
-              <label>학기 종료<input type="date" value={data.settings.termEnd} onChange={event => edit({ termEnd: event.target.value })} /></label>
-            </div>
-            <label className="switch-row">
-              <input
-                type="checkbox"
-                checked={data.settings.useHolidays !== false}
-                onChange={event => edit({ useHolidays: event.target.checked })}
-              />
-              대한민국 공휴일을 달력에 표시하고 그날 수업을 자동으로 뺍니다
-            </label>
-          </div>
-
           <div className="block">
             <h2>백업</h2>
             <p className="hint">
@@ -93,8 +75,15 @@ export default function SettingsHub({ data, update, onFiles, onImport, onReset }
               {APP_NAME} {APP_VERSION}<br />
               {APP_TAGLINE}<br />
               © 2026 율쌤 ｜ 무단 배포 및 수정 금지<br />
-              쓰는 방법은 위 <b>도움말</b> 탭에 정리돼 있습니다.
+              쓰는 방법은 <b>학사일정</b> 탭 옆 <b>도움말</b>에 정리돼 있습니다.
             </p>
+            <div className="sns-links">
+              {links.map(item => (
+                <a className="sns-link" key={item.label} href={item.url} target="_blank" rel="noreferrer">
+                  {item.label}
+                </a>
+              ))}
+            </div>
           </div>
         </section>
       )}
diff --git a/src/exportPrint.ts b/src/exportPrint.ts
index 1228424..c45155a 100644
--- a/src/exportPrint.ts
+++ b/src/exportPrint.ts
@@ -75,7 +75,7 @@ export function exportCalendarXlsx(data: AppData, sessions: Session[], month: Da
   XLSX.writeFile(workbook, `달력_${year}년${monthNumber}월.xlsx`)
 }
 
-/** 한 학급 전체 학생의 출결·특이사항 기록을 한 시트로 내려받는다. */
+/** 한 학급 전체 학생의 출결·특기사항 누가기록을 한 시트로 내려받는다. */
 export function exportClassDigestXlsx(data: AppData, sessions: Session[], classId: string) {
   const classroom = data.classes.find(item => item.id === classId)
   if (!classroom) return
@@ -84,7 +84,7 @@ export function exportClassDigestXlsx(data: AppData, sessions: Session[], classI
     .filter(item => item.classId === classId && item.mode !== 'none' && !item.cancelled)
     .sort((left, right) => (left.date + String(left.period)).localeCompare(right.date + String(right.period)))
 
-  const rows: (string | number)[][] = [['학번', '성명', '날짜', '교시', '수업 내용', '출결', '특이사항']]
+  const rows: (string | number)[][] = [['학번', '성명', '날짜', '교시', '수업 내용', '출결', '특기사항']]
   classroom.students.forEach(student => {
     own.forEach(session => {
       const key = `${session.id}:${student.id}`
@@ -104,6 +104,34 @@ export function exportClassDigestXlsx(data: AppData, sessions: Session[], classI
   XLSX.writeFile(workbook, `출결기록_${classroom.name}_${stamp}.xlsx`)
 }
 
+/** 학생 한 명의 출결·특기사항 누가기록만 내려받는다. */
+export function exportStudentDigestXlsx(data: AppData, sessions: Session[], classId: string, studentId: string) {
+  const classroom = data.classes.find(item => item.id === classId)
+  const student = classroom?.students.find(item => item.id === studentId)
+  if (!classroom || !student) return
+
+  const own = sessions
+    .filter(item => item.classId === classId && item.mode !== 'none' && !item.cancelled)
+    .sort((left, right) => (left.date + String(left.period)).localeCompare(right.date + String(right.period)))
+
+  const rows: (string | number)[][] = [['학번', '성명', '날짜', '교시', '수업 내용', '출결', '특기사항']]
+  own.forEach(session => {
+    const key = `${session.id}:${student.id}`
+    const status = data.attendance[key] || '출석'
+    const note = data.activities[key] || ''
+    if (status === '출석' && !note) return
+    rows.push([student.number, student.name, session.date, session.period, sessionLabel(data, session), status, note])
+  })
+
+  const sheet = XLSX.utils.aoa_to_sheet(rows)
+  sheet['!cols'] = [{ wch: 6 }, { wch: 8 }, { wch: 12 }, { wch: 6 }, { wch: 20 }, { wch: 8 }, { wch: 34 }]
+  const workbook = XLSX.utils.book_new()
+  XLSX.utils.book_append_sheet(workbook, sheet, student.name.slice(0, 31))
+
+  const stamp = new Date().toISOString().slice(0, 10)
+  XLSX.writeFile(workbook, `누가기록_${classroom.name}_${student.name}_${stamp}.xlsx`)
+}
+
 /** 평가 점수를 학급별 시트로 내려받는다. 학교 평가표와 비슷한 형태(학번·성명·요소별 점수·합계·비고). */
 export function exportEvaluationXlsx(data: AppData, evaluation: Evaluation) {
   const classes = evaluationClasses(data, evaluation)
diff --git a/src/naesAttendanceParser.ts b/src/naesAttendanceParser.ts
index 5a3c898..4ab2cea 100644
--- a/src/naesAttendanceParser.ts
+++ b/src/naesAttendanceParser.ts
@@ -61,6 +61,52 @@ function readTeacher(value: string) {
   return matched ? matched[1].trim() : ''
 }
 
+/**
+ * 0순위: `학년`/`반`/`번호`가 각각 다른 열로 나뉜 표를 읽는다.
+ * 선택과목처럼 여러 원적반 학생이 한 반에 섞이면 나이스 출석부가 이 형태로 나온다.
+ * 이때 '번호'는 그 반 안에서만 고유해서(다른 원적반 학생과 번호가 겹칠 수 있다),
+ * 학년+반+번호를 합쳐 5자리 학번(예: 2학년 1반 4번 → 20104)으로 새로 만들어야 학생이 안 겹친다.
+ */
+function findGradeClassNumberColumns(rows: Row[]) {
+  const index = rows.findIndex(row => {
+    const values = row.map(text)
+    return (
+      values.some(value => /^학년$/.test(value))
+      && values.some(value => /^반$/.test(value))
+      && values.some(value => /^번호$|^No\.?$/i.test(value))
+      && values.some(value => /성\s*명|이름/.test(value))
+    )
+  })
+  if (index < 0) return null
+  const header = rows[index].map(text)
+  const gradeIndex = header.findIndex(value => /^학년$/.test(value))
+  const classIndex = header.findIndex(value => /^반$/.test(value))
+  const noIndex = header.findIndex(value => /^번호$|^No\.?$/i.test(value))
+  const nameIndex = header.findIndex(value => /^성\s*명$|^이름$|성명/.test(value))
+  if ([gradeIndex, classIndex, noIndex, nameIndex].some(item => item < 0)) return null
+  return { index, gradeIndex, classIndex, noIndex, nameIndex }
+}
+
+function collectStudentsFromGradeClassNumber(
+  rows: Row[],
+  cols: { index: number; gradeIndex: number; classIndex: number; noIndex: number; nameIndex: number },
+) {
+  const students: ImportedStudent[] = []
+  rows.slice(cols.index + 1).forEach(row => {
+    const name = text(row[cols.nameIndex])
+    if (!name || !isHangulName(name)) return
+    const grade = Number(text(row[cols.gradeIndex]).replace(/[^0-9]/g, ''))
+    const classNo = Number(text(row[cols.classIndex]).replace(/[^0-9]/g, ''))
+    const no = Number(text(row[cols.noIndex]).replace(/[^0-9]/g, ''))
+    if (!grade || !classNo || !no) return
+    // 학년 1자리 + 반 2자리 + 번호 2자리 = 5자리. 원적반이 서로 달라도 겹치지 않는다.
+    const number = grade * 10000 + classNo * 100 + no
+    if (students.some(item => item.number === number)) return
+    students.push({ number, name })
+  })
+  return students.sort((left, right) => left.number - right.number)
+}
+
 /**
  * 1순위: `학번`/`번호`/`No.` + `성명`/`이름` 머리글을 찾는다.
  * NEIS 출석부는 열이 'No.'(반 출석번호)와 '학번'(전체 학번)으로 나뉘어 있는 경우가 많다.
@@ -171,8 +217,13 @@ function parseSheet(sheet: XLSX.WorkSheet, fileName: string, sheetName: string):
   }
   if (!className) className = readClassOnly(sheetName) || readClassOnly(fileName)
 
-  const columns = findHeaderColumns(rows) || guessColumns(rows)
-  const students = columns ? collectStudents(rows, columns.index, columns.numberIndex, columns.nameIndex) : []
+  const splitColumns = findGradeClassNumberColumns(rows)
+  const columns = splitColumns ? null : (findHeaderColumns(rows) || guessColumns(rows))
+  const students = splitColumns
+    ? collectStudentsFromGradeClassNumber(rows, splitColumns)
+    : columns
+      ? collectStudents(rows, columns.index, columns.numberIndex, columns.nameIndex)
+      : []
 
   if (!className && !students.length && !slots.length) return null
 
